{
  "name": "strands-agent-infra",
  "version": "1.0.0",
  "description": "CDK infrastructure for Strands Agent with Streamlit on EC2 and CloudFront",
  "main": "lib/strands-agent-stack.js",
  "scripts": {
    "build": "echo 'No build needed for JS'",
    "cdk": "cdk",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy",
    "synth": "cdk synth"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.0.0"
  }
}
